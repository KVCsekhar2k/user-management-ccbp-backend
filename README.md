# User Management API

## Summary
Simple Node.js + Express REST API to manage users with SQLite storage. Implements create, get, delete, update endpoints with validation and logging.

## Requirements
See assignment PDF. (This project implements the endpoints `/create_user`, `/get_users`, `/delete_user`, `/update_user`.)

## Setup
1. `npm install`
2. create `.env` with:
